---
# PSS-SNAPSHOT: k8s 1.36 / pod-security-admission policy version v1.35 / check seLinuxOptions
#
# LAYER 1 PURE MIRROR. The conventions are stated once, in best-practices/restrict-node-port.yaml
# and ../restricted/restrict-volume-types.yaml; this file follows them.
#
# `container_engine_t` ENTERED THE STANDARD AT PSS POLICY VERSION v1.31, as
# `selinuxAllowedTypes1_31` in check_seLinuxOptions.go. Any source transcribing an earlier
# revision carries the other three types only, so resyncing this allow-list from one silently
# narrows it -- the same trap `image` carries in ../restricted/restrict-volume-types.yaml. The
# type allow-list is the only version-gated half of this policy: `seLinuxOptions.user` and
# `.role` must be empty at every level at every policy version, which is what the second
# `validations[]` entry holds.
#
# `.orValue('container_t')` stands in for "field absent", it is NOT an API default -- the API
# defaults nothing here and the standard permits the field being unset. Same shape in
# restrict-seccomp.yaml.
apiVersion: policies.kyverno.io/v1
kind: ValidatingPolicy
metadata:
  name: disallow-selinux
  annotations:
    policies.kyverno.io/title: Disallow SELinux
    policies.kyverno.io/category: Pod Security Standards (Baseline)
    policies.kyverno.io/severity: medium
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/minversion: 1.17.0
    kyverno.io/kubernetes-version: "1.36"
    policies.kyverno.io/description: >-
      SELinux options can be used to escalate privileges and should not be allowed. This policy
      ensures that the `seLinuxOptions` field is undefined.
spec:
  autogen:
    podControllers:
      controllers: []
  evaluation:
    background:
      enabled: true
  matchConditions: []
  matchConstraints:
    resourceRules:
    - apiGroups:
      - ''
      apiVersions:
      - v1
      operations:
      - CREATE
      - UPDATE
      resources:
      - pods
  validationActions:
  - Audit
  validations:
  - expression: >-
      object.spec.?securityContext.?seLinuxOptions.?type.orValue('container_t') in
      ['container_t', 'container_init_t', 'container_kvm_t', 'container_engine_t'] &&
      (object.spec.containers +
      object.spec.?initContainers.orValue([]) +
      object.spec.?ephemeralContainers.orValue([])).all(c,
      c.?securityContext.?seLinuxOptions.?type.orValue('container_t') in
      ['container_t', 'container_init_t', 'container_kvm_t', 'container_engine_t'])
    message: >-
      Setting the SELinux type is restricted. The fields
      spec.securityContext.seLinuxOptions.type,
      spec.containers[*].securityContext.seLinuxOptions.type,
      spec.initContainers[*].securityContext.seLinuxOptions.type, and
      spec.ephemeralContainers[*].securityContext.seLinuxOptions.type
      must either be unset or set to one of the allowed values
      (container_t, container_init_t, container_kvm_t, or container_engine_t).
  - expression: >-
      object.spec.?securityContext.?seLinuxOptions.?user.orValue('') == '' &&
      object.spec.?securityContext.?seLinuxOptions.?role.orValue('') == '' &&
      (object.spec.containers +
      object.spec.?initContainers.orValue([]) +
      object.spec.?ephemeralContainers.orValue([])).all(c,
      c.?securityContext.?seLinuxOptions.?user.orValue('') == '' &&
      c.?securityContext.?seLinuxOptions.?role.orValue('') == '')
    message: >-
      Setting the SELinux user or role is forbidden. The fields
      spec.securityContext.seLinuxOptions.user,
      spec.securityContext.seLinuxOptions.role,
      spec.containers[*].securityContext.seLinuxOptions.user,
      spec.containers[*].securityContext.seLinuxOptions.role,
      spec.initContainers[*].securityContext.seLinuxOptions.user,
      spec.initContainers[*].securityContext.seLinuxOptions.role,
      spec.ephemeralContainers[*].securityContext.seLinuxOptions.user, and
      spec.ephemeralContainers[*].securityContext.seLinuxOptions.role
      must be unset.
