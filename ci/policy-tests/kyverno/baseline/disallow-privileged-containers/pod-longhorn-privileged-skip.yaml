---
# EXPECTED: `skip` against the built policy, `fail` against the pure mirror.
#
# Fires `exempt-node-level-namespaces`, second member.
apiVersion: v1
kind: Pod
metadata:
  name: longhorn-manager-rg2fl
  namespace: longhorn-system
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
    securityContext:
      privileged: true
