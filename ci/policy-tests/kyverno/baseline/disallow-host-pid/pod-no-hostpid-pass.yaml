---
# EXPECTED: `pass` against both the built policy and the pure mirror. The field is absent, which
# the rule allows -- see disallow-host-network's pod-no-hostnetwork-pass.yaml for why a fixture
# omitting an optional field is the normal shape and why the CEL is written to tolerate it.
apiVersion: v1
kind: Pod
metadata:
  name: compliant-workload-6b9f4c8d5-x2kqp
  namespace: default
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
