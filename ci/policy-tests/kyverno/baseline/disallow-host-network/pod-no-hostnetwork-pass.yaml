---
# EXPECTED: `pass` against both the built policy and the pure mirror.
#
# `pass`, NOT `skip`: this pod is evaluated and satisfies the rule. The field is absent entirely,
# which is the shape the legacy conditional anchor `=(hostNetwork): "false"` tolerated and which
# `object.spec.?hostNetwork.orValue(false)` reproduces -- a fixture omitting the field is the
# normal way to write "an ordinary pod", and CEL has no defaulting step of its own.
apiVersion: v1
kind: Pod
metadata:
  name: compliant-workload-6b9f4c8d5-x2kqp
  namespace: default
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
