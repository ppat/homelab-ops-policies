---
# EXPECTED: `fail` against both the built policy and the pure mirror.
#
# THE LOAD-BEARING CONTROL OF THIS DIRECTORY. Every `skip` row in kyverno-test.yaml asserts that
# some workload is exempted from a rule; none of them means anything unless the rule still
# objects to an ordinary pod. `default` is a namespace no exemption in this repo names.
apiVersion: v1
kind: Pod
metadata:
  name: ordinary-workload-5f7d9b46c-tn4wq
  namespace: default
spec:
  hostNetwork: true
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
