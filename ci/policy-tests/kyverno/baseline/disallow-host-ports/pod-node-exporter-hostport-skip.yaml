---
# EXPECTED: `skip` against the built policy, `fail` against the pure mirror.
#
# FIRING FIXTURE for the exempt-node-exporter matchCondition. The name below is the full
# release-derived name the exemption's prefix is written against; a shortened stand-in would keep
# this suite green while no longer describing the workload the exemption exists for.
apiVersion: v1
kind: Pod
metadata:
  name: kube-prometheus-stack-prometheus-node-exporter-7g4hj
  namespace: monitoring
spec:
  restartPolicy: Always
  containers:
  - name: node-exporter
    image: quay.io/prometheus/node-exporter:v1.8.0
    ports:
    - containerPort: 9100
      hostPort: 9100
