---
# EXPECTED: `skip`.
#
# The second namespace in the same exemption entry, falsified independently: a fixture for only one
# member would stay green if the other were dropped from the list. Same non-vacuity shape as its
# kube-system twin -- no requests declared, so the policy would otherwise mutate it.
apiVersion: v1
kind: Pod
metadata:
  name: longhorn-manager-ish
  namespace: longhorn-system
spec:
  containers:
  - name: app
    image: nginx:1.29
