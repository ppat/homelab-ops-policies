---
# EXPECTED: `fail`.
#
# The core assertion of the redesign, kind 3 of 3 -- StatefulSet, matched directly.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres
  namespace: default
spec:
  serviceName: postgres
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: registry.internal/postgres:16.4
