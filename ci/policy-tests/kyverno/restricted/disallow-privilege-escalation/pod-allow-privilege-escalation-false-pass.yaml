---
# EXPECTED: `pass`. The only compliant shape there is -- explicitly false on every container in
# all three lists.
apiVersion: v1
kind: Pod
metadata:
  name: compliant-workload-6b9f4c8d5-x2kqp
  namespace: default
spec:
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
    securityContext:
      allowPrivilegeEscalation: false
  - name: sidecar
    image: registry.k8s.io/pause:3.10
    securityContext:
      allowPrivilegeEscalation: false
