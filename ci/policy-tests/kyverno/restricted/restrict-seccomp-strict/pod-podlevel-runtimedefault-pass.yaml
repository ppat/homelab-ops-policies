---
# EXPECTED: `pass`.
#
# INHERITANCE, ASSERTED: the pod sets `RuntimeDefault` and the containers say nothing, so the
# effective per-container value is the pod's -- which is why the mirror's inner
# `.orValue(<the pod-level value>)` exists. An expression that only concatenated the container
# lists would flag this pod.
apiVersion: v1
kind: Pod
metadata:
  name: podlevel-rd-workload-6b9f4c8d5-x2kqp
  namespace: default
spec:
  securityContext:
    seccompProfile:
      type: RuntimeDefault
  containers:
  - name: app
    image: registry.k8s.io/pause:3.10
  - name: bare
    image: registry.k8s.io/pause:3.10
