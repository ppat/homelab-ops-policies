---
# EXPECTED: `fail` against both documents.
#
# HALF-MATCH CONTROL, namespace half: right namespace, wrong name. Without it, an exemption that
# had lost its name predicate -- exempting all of `kubevirt` -- would look identical to a correct
# one. virt-api is a real KubeVirt component and is deliberately not exempted anywhere in this
# repo.
apiVersion: v1
kind: Pod
metadata:
  name: virt-api-6d9c8f5b7-9wq4x
  namespace: kubevirt
spec:
  containers:
  - name: virt-api
    image: registry.k8s.io/pause:3.10
