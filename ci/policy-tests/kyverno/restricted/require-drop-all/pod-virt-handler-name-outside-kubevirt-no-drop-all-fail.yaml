---
# EXPECTED: `fail` against both documents.
#
# HALF-MATCH CONTROL, name half: right name, wrong namespace. Without it, an exemption that had
# lost its namespace predicate -- exempting anything called virt-handler* anywhere -- would look
# identical to a correct one.
apiVersion: v1
kind: Pod
metadata:
  name: virt-handler-zzz11
  namespace: monitoring
spec:
  containers:
  - name: virt-handler
    image: registry.k8s.io/pause:3.10
