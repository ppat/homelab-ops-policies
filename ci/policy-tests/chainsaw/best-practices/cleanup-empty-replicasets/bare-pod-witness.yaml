---
# THE "A CLEANUP CYCLE GENUINELY RAN" ANCHOR -- see chainsaw-test.yaml's header for the full
# reasoning. This Pod carries no ownerReferences, so `cleanup-bare-pods` (which has no age guard
# at all) will delete it on its very first cycle once its schedule is patched fast. Its deletion
# is the live, non-vacuous proof that the cleanup-controller machinery -- RBAC, resourceFilters,
# schedule, engine -- is actually working in this test run, which is what makes
# fresh-empty-replicaset.yaml's continued EXISTENCE next to it a real "survived", not merely
# "nobody checked".
apiVersion: v1
kind: Pod
metadata:
  name: bare-pod-witness
  namespace: default
spec:
  containers:
  - name: c
    image: registry.k8s.io/pause:3.10
